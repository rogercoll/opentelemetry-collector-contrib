[comment]: <> (Code generated by mdatagen. DO NOT EDIT.)

# pprofmetrics

## Default Metrics

The following metrics are emitted by default. Each of them can be disabled by applying the following configuration:

```yaml
metrics:
  <metric_name>:
    enabled: false
```

### pprof.profile.duration_nanoseconds

Wall duration of the profile observation window in nanoseconds.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| ns | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level |
| ---- | ----------- | ------ | -------- |
| period_type | Period type label (e.g. cpu, wall). | Any Str | Recommended |
| period_unit | Unit for the sampling period. | Any Str | Recommended |
| profile_id | Unique key for the profile slice (hex profile ID, or a synthetic scope/profile index if unset). | Any Str | Recommended |
| sample_type | Sample type label from the profile (e.g. cpu, inuse_space, alloc_space). | Any Str | Recommended |
| sample_unit | Unit for the sample type (e.g. nanoseconds, bytes, count). | Any Str | Recommended |

### pprof.profile.frame_type_samples

Count of stack samples attributed to each profile.frame.type on frames in the stack.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| {sample} | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level |
| ---- | ----------- | ------ | -------- |
| frame_type | Semantic convention profile.frame.type value for a stack frame. | Any Str | Recommended |
| period_type | Period type label (e.g. cpu, wall). | Any Str | Recommended |
| period_unit | Unit for the sampling period. | Any Str | Recommended |
| profile_id | Unique key for the profile slice (hex profile ID, or a synthetic scope/profile index if unset). | Any Str | Recommended |
| sample_origin | Combined sample type and unit for grouping (type_unit). | Any Str | Recommended |
| sample_type | Sample type label from the profile (e.g. cpu, inuse_space, alloc_space). | Any Str | Recommended |
| sample_unit | Unit for the sample type (e.g. nanoseconds, bytes, count). | Any Str | Recommended |

### pprof.profile.period

Profile sampling period in native units (often nanoseconds for CPU profiles).

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level |
| ---- | ----------- | ------ | -------- |
| period_type | Period type label (e.g. cpu, wall). | Any Str | Recommended |
| period_unit | Unit for the sampling period. | Any Str | Recommended |
| profile_id | Unique key for the profile slice (hex profile ID, or a synthetic scope/profile index if unset). | Any Str | Recommended |
| sample_type | Sample type label from the profile (e.g. cpu, inuse_space, alloc_space). | Any Str | Recommended |
| sample_unit | Unit for the sample type (e.g. nanoseconds, bytes, count). | Any Str | Recommended |

### pprof.profile.sample_count

Number of samples in the profile.

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| {sample} | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level |
| ---- | ----------- | ------ | -------- |
| period_type | Period type label (e.g. cpu, wall). | Any Str | Recommended |
| period_unit | Unit for the sampling period. | Any Str | Recommended |
| profile_id | Unique key for the profile slice (hex profile ID, or a synthetic scope/profile index if unset). | Any Str | Recommended |
| sample_type | Sample type label from the profile (e.g. cpu, inuse_space, alloc_space). | Any Str | Recommended |
| sample_unit | Unit for the sample type (e.g. nanoseconds, bytes, count). | Any Str | Recommended |

### pprof.profile.sample_value_sum

Sum of sample Values() across all samples in the profile (e.g. total CPU ns, allocated bytes).

| Unit | Metric Type | Value Type | Stability |
| ---- | ----------- | ---------- | --------- |
| 1 | Gauge | Int | Development |

#### Attributes

| Name | Description | Values | Requirement Level |
| ---- | ----------- | ------ | -------- |
| period_type | Period type label (e.g. cpu, wall). | Any Str | Recommended |
| period_unit | Unit for the sampling period. | Any Str | Recommended |
| profile_id | Unique key for the profile slice (hex profile ID, or a synthetic scope/profile index if unset). | Any Str | Recommended |
| sample_type | Sample type label from the profile (e.g. cpu, inuse_space, alloc_space). | Any Str | Recommended |
| sample_unit | Unit for the sample type (e.g. nanoseconds, bytes, count). | Any Str | Recommended |
