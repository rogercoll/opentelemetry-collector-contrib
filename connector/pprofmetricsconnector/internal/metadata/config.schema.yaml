# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for pprofmetrics metrics.
    type: object
    properties:
      pprof.profile.duration_nanoseconds:
        description: "PprofProfileDurationNanosecondsMetricConfig provides config for the pprof.profile.duration_nanoseconds metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pprof.profile.frame_type_samples:
        description: "PprofProfileFrameTypeSamplesMetricConfig provides config for the pprof.profile.frame_type_samples metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pprof.profile.period:
        description: "PprofProfilePeriodMetricConfig provides config for the pprof.profile.period metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pprof.profile.sample_count:
        description: "PprofProfileSampleCountMetricConfig provides config for the pprof.profile.sample_count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      pprof.profile.sample_value_sum:
        description: "PprofProfileSampleValueSumMetricConfig provides config for the pprof.profile.sample_value_sum metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for pprofmetrics metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
